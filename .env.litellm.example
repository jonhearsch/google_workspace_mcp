# ============================================
# LiteLLM + Google Workspace MCP - Environment Configuration
# ============================================
#
# Copy this file to .env and configure with your credentials
# cp .env.litellm.example .env

# ============================================
# Required: Google OAuth Credentials
# ============================================
# Get these from: https://console.cloud.google.com/apis/credentials
# See README.md for setup instructions
GOOGLE_OAUTH_CLIENT_ID=your-client-id-here.apps.googleusercontent.com
GOOGLE_OAUTH_CLIENT_SECRET=your-client-secret-here

# Your Google account email (single-user mode)
USER_GOOGLE_EMAIL=your.email@gmail.com

# ============================================
# LiteLLM Configuration
# ============================================
# Master key for securing the LiteLLM proxy
# Change this to a secure random value!
LITELLM_MASTER_KEY=sk-1234-change-this-to-something-secure

# Port for LiteLLM proxy
LITELLM_PORT=4000

# ============================================
# LLM Provider API Keys
# ============================================
# Add keys for the providers you want to use
# At least one is required

# OpenAI
OPENAI_API_KEY=sk-...

# Anthropic (Claude)
# ANTHROPIC_API_KEY=sk-ant-...

# Google Gemini
# GEMINI_API_KEY=...

# Cohere
# COHERE_API_KEY=...

# Azure OpenAI
# AZURE_API_KEY=...
# AZURE_API_BASE=https://your-resource.openai.azure.com
# AZURE_API_VERSION=2024-02-15-preview

# ============================================
# Optional: Local Models (Ollama)
# ============================================
# If running Ollama on your host machine
OLLAMA_API_BASE=http://host.docker.internal:11434

# If running Ollama in Docker (uncomment ollama service in docker-compose)
# OLLAMA_API_BASE=http://ollama:11434

# ============================================
# Optional: Google Workspace Tool Selection
# ============================================
# Method 1: Use tool tiers (recommended)
# Options: core, extended, complete
TOOL_TIER=core

# Method 2: Specify individual tools (uncomment to use)
# Options: gmail, drive, calendar, docs, sheets, slides, forms, tasks, chat, search
# TOOLS=gmail,drive,calendar

# ============================================
# Optional: Google Custom Search
# ============================================
# Get API key from: https://developers.google.com/custom-search/v1/overview
# Get Engine ID from: https://programmablesearchengine.google.com/controlpanel/create
# GOOGLE_PSE_API_KEY=your-api-key-here
# GOOGLE_PSE_ENGINE_ID=your-engine-id-here

# ============================================
# Optional: PostgreSQL Database (for logging)
# ============================================
# Uncomment postgres service in docker-compose.litellm.yml to enable
# DATABASE_URL=postgresql://litellm:litellm_password@postgres:5432/litellm
# POSTGRES_PASSWORD=litellm_password

# ============================================
# Optional: Redis (for caching)
# ============================================
# Uncomment redis service in docker-compose.litellm.yml to enable
# REDIS_HOST=redis
# REDIS_PORT=6379
# REDIS_PASSWORD=

# ============================================
# Optional: LiteLLM UI Access
# ============================================
# Access UI at http://localhost:4000/ui
UI_USERNAME=admin
UI_PASSWORD=admin

# ============================================
# Advanced: Google Workspace MCP Settings
# ============================================
# Typically not needed, defaults are fine

# Server base URI (no port)
# WORKSPACE_MCP_BASE_URI=http://localhost

# Credential storage directory
# GOOGLE_MCP_CREDENTIALS_DIR=/app/store_creds

# ============================================
# Production Settings (HTTPS/Reverse Proxy)
# ============================================
# Only needed if running behind a reverse proxy

# External URL for OAuth callbacks
# WORKSPACE_EXTERNAL_URL=https://your-domain.com

# Or just override OAuth redirect URI
# GOOGLE_OAUTH_REDIRECT_URI=https://your-domain.com/oauth2callback
