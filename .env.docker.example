# ============================================
# Google Workspace MCP - Docker Environment
# ============================================
#
# Copy this file to .env and configure with your credentials
# cp .env.docker.example .env

# ============================================
# Required: Google OAuth Credentials
# ============================================
# Get these from: https://console.cloud.google.com/apis/credentials
GOOGLE_OAUTH_CLIENT_ID=your-client-id-here.apps.googleusercontent.com
GOOGLE_OAUTH_CLIENT_SECRET=your-client-secret-here

# ============================================
# Development Settings
# ============================================
# Required for development (allows http redirect URIs)
OAUTHLIB_INSECURE_TRANSPORT=1

# Server port (default: 8000)
PORT=8000

# ============================================
# Optional: Single User Configuration
# ============================================
# Set a default email to skip email prompts in single-user mode
# USER_GOOGLE_EMAIL=your.email@gmail.com

# ============================================
# Optional: Tool Selection
# ============================================
# Method 1: Use tool tiers (recommended)
# Options: core, extended, complete
# TOOL_TIER=core

# Method 2: Specify individual tools (comma-separated)
# Options: gmail, drive, calendar, docs, sheets, slides, forms, tasks, chat, search
# TOOLS=gmail,drive,calendar

# ============================================
# Optional: Google Custom Search
# ============================================
# Get API key from: https://developers.google.com/custom-search/v1/overview
# Get Engine ID from: https://programmablesearchengine.google.com/controlpanel/create
# GOOGLE_PSE_API_KEY=your-api-key-here
# GOOGLE_PSE_ENGINE_ID=your-engine-id-here

# ============================================
# Optional: OAuth 2.1 Multi-User Support
# ============================================
# Enable OAuth 2.1 for multi-user bearer token authentication
# MCP_ENABLE_OAUTH21=false

# Use external OAuth provider (requires MCP_ENABLE_OAUTH21=true)
# EXTERNAL_OAUTH21_PROVIDER=false

# Enable stateless mode for containers (requires MCP_ENABLE_OAUTH21=true)
# WORKSPACE_MCP_STATELESS_MODE=false

# ============================================
# Optional: OAuth Proxy Storage Backend
# ============================================
# Options: memory, disk, valkey
# WORKSPACE_MCP_OAUTH_PROXY_STORAGE_BACKEND=memory

# Disk storage configuration
# WORKSPACE_MCP_OAUTH_PROXY_DISK_DIRECTORY=~/.fastmcp/oauth-proxy

# Valkey/Redis configuration (if using valkey backend)
# WORKSPACE_MCP_OAUTH_PROXY_VALKEY_HOST=localhost
# WORKSPACE_MCP_OAUTH_PROXY_VALKEY_PORT=6379
# WORKSPACE_MCP_OAUTH_PROXY_VALKEY_DB=0
# WORKSPACE_MCP_OAUTH_PROXY_VALKEY_USE_TLS=auto
# WORKSPACE_MCP_OAUTH_PROXY_VALKEY_USERNAME=
# WORKSPACE_MCP_OAUTH_PROXY_VALKEY_PASSWORD=
# WORKSPACE_MCP_OAUTH_PROXY_VALKEY_REQUEST_TIMEOUT_MS=5000
# WORKSPACE_MCP_OAUTH_PROXY_VALKEY_CONNECTION_TIMEOUT_MS=10000

# ============================================
# Optional: Reverse Proxy Configuration
# ============================================
# If running behind a reverse proxy, set your external URL
# WORKSPACE_EXTERNAL_URL=https://your-domain.com

# Or just override the OAuth redirect URI
# GOOGLE_OAUTH_REDIRECT_URI=https://your-domain.com/oauth2callback

# Additional redirect URIs (comma-separated)
# OAUTH_CUSTOM_REDIRECT_URIS=

# Allowed CORS origins (comma-separated)
# OAUTH_ALLOWED_ORIGINS=

# ============================================
# Optional: Server Configuration
# ============================================
# Base server URI (no port)
# WORKSPACE_MCP_BASE_URI=http://localhost
